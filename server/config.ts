import { salonSchedule } from '@/data/schedule'

export { listActiveServices as bookableServices } from '@server/services.js'
export { getService } from '@server/services.js'

export const schedule = salonSchedule
